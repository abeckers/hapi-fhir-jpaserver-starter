apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bundle.fullname" . }}-test-connection"
  labels:
    {{- include "bundle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bundle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
